{
  "name": "turborepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "py-package"
  ],
  "scripts": {
    "dev": "turbo run dev --no-cache --parallel --continue"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "turbo": {
    "pipeline": {
      "dev": {
        "cache": false
      }
    }
  }
}
